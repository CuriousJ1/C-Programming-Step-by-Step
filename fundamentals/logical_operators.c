#include <stdio.h>


/*
What are the 3 logical operators

What are the parameters fo AND(&&)?

What are the parametners for NOT(!)

What are the parameter for OR (||)?
*/

int main() {

    int i, age;
    age = 25;
    i = age >= 13 && age <= 19;
    printf("i = %d\n", i);
    return 0;
}